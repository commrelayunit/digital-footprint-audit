# Examples

Do not place real mailbox or password exports here.

For a quick mbox smoke test, create a tiny synthetic mbox outside the repo or under `data/` locally; `data/` is gitignored.

For Firefox login import smoke tests, use only synthetic CSV rows under gitignored `data/`. Never use real exported passwords in examples, tests, commits, screenshots, or issue comments.
